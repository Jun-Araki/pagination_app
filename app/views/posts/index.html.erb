<%= form_with model: @post, local: true do |form| %>
  <%= form.label :body, "内容" %>
  <%= form.text_field :body, required: true %>
  <%= form.submit "投稿" %>
<% end %>
<h1>投稿一覧</h1>
<%= render "form" %>
<%= render @posts %>
<%= paginate @posts %>
